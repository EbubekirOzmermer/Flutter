import 'package:flutter/material.dart';

class Kazanc extends StatelessWidget {
  const Kazanc({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Padding(
        padding: const EdgeInsets.only(top: 50.0),
        child: SizedBox(),
      ),
    );
  }
}
